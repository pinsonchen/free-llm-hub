"""`freellm` CLI placeholder. M1 will replace this with click/typer-based commands
(`doctor`, `models`, `route`, `registry verify`)."""

import sys


def main() -> int:
    print("freellm CLI is not implemented yet — see docs/ROADMAP.md (M1).")
    return 0


if __name__ == "__main__":
    sys.exit(main())
